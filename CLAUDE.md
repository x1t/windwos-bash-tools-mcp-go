# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

**MCP Bash Tools** - 专为Windows设计的MCP服务器，提供企业级安全的PowerShell命令执行能力。项目采用Go 1.23.0开发，仅支持PowerShell 7和PowerShell 5.x。

## 🎯 核心设计理念

### 超时与后台执行机制

**前台执行（run_in_background=false）**:
- 命令在timeout时间内完成 → 立即返回结果
- 命令超过timeout时间 → **自动转为后台任务**，返回任务ID，命令继续执行
- Agent不会被长时间阻塞，可以继续处理其他请求

**后台执行（run_in_background=true）**:
- **无超时限制** - 任务可以运行任意长时间
- 用户通过 `bash_output` 工具查看进度
- 用户通过 `kill_shell` 工具手动终止

**设计原则**:
1. ✅ 前台超时不等于失败，而是转为后台继续执行
2. ✅ 后台任务永不超时，由用户主动控制
3. ✅ Agent永不被长时间阻塞
4. ✅ 所有任务都可以通过bash_output查看进度

## 常用命令

```powershell
# 构建（调试模式）
.\build.ps1

# 发布模式（优化二进制文件）
.\build.ps1 -Release

# 运行服务器
.\dist\bash-tools.exe

# 运行所有测试
go test ./...

# 并发安全测试
go test -race ./...

# 生成覆盖率报告
go test -coverprofile=coverage.out ./...
go tool cover -html=coverage.out -o coverage.html

# 静态分析
go vet ./...

# 格式化代码
go fmt ./...
```

## 核心架构设计

### 三层架构模式

```
┌─────────────────────────────────────┐
│          MCP接口层 (cmd/server)        │
│  • 工具注册与JSON-RPC通信             │
│  • 参数验证与错误处理                 │
└─────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────┐
│        业务逻辑层 (internal)          │
│  • executor/  - Shell执行器          │
│  • security/  - 安全验证             │
│  • core/      - 类型定义             │
└─────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────┐
│      基础设施层 (pkg)                 │
│  • logger/  - 日志系统                │
│  • utils/   - 工具函数                │
└─────────────────────────────────────┘
```

### 核心模块职责

#### cmd/server/main.go - MCP服务器核心
- **MCP协议实现**: 工具注册、JSON-RPC通信
- **三个核心工具**: `bash`, `bash_output`, `kill_shell`
- **后台任务管理**: 使用`sync.RWMutex`保证并发安全
- **临时文件机制**: 实时存储后台任务输出

#### internal/executor/ - Shell执行器系统
- **shell.go**: 智能Shell检测，优先级：PowerShell 7 → PowerShell 5.x
- **bash.go**: 基础命令执行，支持超时控制和进程管理

#### internal/security/ - 安全防护体系
- **validator.go**: 危险模式识别（Windows专用）、PowerShell引号检测、命令长度限制

## 安全机制

### 多层安全防护

1. **输入验证**: 参数类型检查、长度限制、特殊字符过滤
2. **命令验证**: 70+种Windows危险模式识别、正则表达式匹配
3. **执行隔离**: 工作目录限制、临时文件管理
4. **超时保护**: 强制超时控制（1-600秒）
5. **监控审计**: 命令执行记录、错误日志

### 危险命令分类 (Windows专用)
- **系统破坏**: `del /s`, `format`, `diskpart`
- **系统控制**: `shutdown`, `reboot`, `Stop-Computer`
- **权限提升**: `takeown`, `net user /add`, `icacls`
- **网络攻击**: `net use`, `bitsadmin`, `certutil`
- **恶意下载**: `downloadstring`, `downloadfile`, `Invoke-Expression`

**注意**: 此项目仅支持Windows PowerShell，已移除所有Linux专用命令检测（如`rm -rf /`, `iptables`, `sudo`等）。

## MCP工具接口

### Bash工具
执行PowerShell命令，支持前台和后台模式。

**超时行为（重要）**:
- **前台执行**: 命令在timeout内完成则返回结果；超时则自动转为后台任务，返回任务ID
- **后台执行**: 无超时限制，持续运行直到完成或被手动kill

**参数**:
- `command` (string, 必填): 要执行的PowerShell命令
- `timeout` (int, 必填): 超时时间(毫秒)，范围1000-600000
- `description` (string, 可选): 命令描述
- `run_in_background` (bool, 必填): 是否后台执行

**返回**:
- `output` (string): 命令输出
- `exitCode` (int): 退出代码
- `killed` (bool): 是否被强制终止
- `shellId` (string): 任务ID（后台模式或前台超时时返回）

**使用示例**:
```json
// 快速命令（5秒内完成）
{
  "command": "Get-Process | Select-Object -First 10",
  "timeout": 5000,
  "run_in_background": false
}

// 长时间任务（后台执行）
{
  "command": "npm run build",
  "timeout": 300000,
  "run_in_background": true
}

// 可能超时的命令（自动转后台）
{
  "command": "dotnet build --configuration Release",
  "timeout": 30000,
  "run_in_background": false
}
// 如果30秒内未完成，自动转后台，返回shellId
```

### BashOutput工具
获取后台任务的实时输出内容，支持正则过滤。

**参数**:
- `bash_id` (string, 必填): 后台任务ID
- `filter` (string, 可选): 正则表达式过滤器

**返回**:
- `output` (string): 输出内容（过滤后）
- `status` (string): 任务状态（running, completed, failed, killed）
- `exitCode` (int, 可选): 退出代码

### KillShell工具
终止后台任务，释放系统资源。

**参数**:
- `shell_id` (string, 必填): 要终止的任务ID

**返回**:
- `message` (string): 操作结果消息
- `shell_id` (string): 被终止的任务ID

## 安全机制

### 六层安全防护体系

1. **认证授权**: JWT + RBAC权限控制
2. **速率限制**: Token Bucket算法，10 RPS，突发20
3. **输入验证**: 危险模式识别
4. **执行隔离**: 可选沙箱环境
5. **资源限制**: CPU/内存/磁盘/文件句柄限制
6. **审计日志**: 结构化安全事件记录

### 危险命令分类 (Windows专用)
- **系统破坏**: `del /s`, `format C:`, `diskpart`, `rd /s /q`
- **系统控制**: `shutdown`, `reboot`, `Stop-Computer`, `Restart-Computer`
- **恶意下载**: `bitsadmin`, `certutil -urlcache`, `downloadstring`
- **权限提升**: `takeown`, `icacls`, `net user /add`, `net localgroup`
- **后门攻击**: `powershell -enc`, `Invoke-Expression`, `mshta`

**注意**: 此项目仅支持Windows PowerShell，所有Linux专用命令检测已被移除。

## 后台任务机制

**BackgroundTask结构**:
```go
type BackgroundTask struct {
    ID        string    `json:"id"`
    Command   string    `json:"command"`
    Output    string    `json:"output"`
    Status    string    `json:"status"` // running, completed, failed, killed
    StartTime time.Time `json:"startTime"`
    Error     string    `json:"error,omitempty"`
    ExitCode  *int      `json:"exitCode,omitempty"`
    TempFile  string    `json:"tempFile,omitempty"`
    Cancel    context.CancelFunc `json:"-"` // 用于kill_shell终止任务
}
```

**并发安全**: 使用`sync.RWMutex`保护后台任务存储，读锁查询，写锁更新。

**超时策略**:
- ✅ 后台任务使用 `context.WithCancel` 而非 `WithTimeout`
- ✅ 任务可以运行任意长时间，由用户通过 `kill_shell` 主动终止
- ✅ 前台任务超时后自动转为后台任务，不会被强制终止

**生命周期**:
1. 创建: 通过 `bash` 工具创建（后台模式或前台超时）
2. 运行: 任务在后台持续执行，输出写入临时文件
3. 查询: 通过 `bash_output` 工具查看进度和输出
4. 终止: 通过 `kill_shell` 工具手动终止，或等待自然完成

## 项目依赖

- `github.com/modelcontextprotocol/go-sdk v1.1.0`: MCP官方SDK
- `github.com/sirupsen/logrus v1.9.3`: 结构化日志
- `github.com/stretchr/testify v1.11.1`: 测试框架

## 添加新功能流程

1. 在`cmd/server/main.go`定义工具输入输出类型（遵循BashArguments命名规范）
2. 在`internal/executor/`实现ShellExecutorInterface接口
3. 在main函数中注册新工具（参考BashHandler实现）
4. 编写对应测试文件（表驱动模式+Mock）
5. 运行`go test -v`验证功能

## 并发安全要求

- 后台任务管理必须使用`sync.RWMutex`
- 共享资源访问需要锁保护
- 读操作使用 `RLock()`/`RUnlock()`
- 写操作使用 `Lock()`/`Unlock()`
- 避免在持锁期间进行I/O操作（如文件读写）

## 超时处理最佳实践

### ✅ 正确做法
```go
// 前台执行 - 使用select等待结果或超时
select {
case result := <-resultChan:
    // 命令完成，返回结果
    return result
case <-time.After(timeout):
    // 超时，转为后台任务
    return convertToBackground()
}

// 后台执行 - 使用WithCancel，不设置超时
ctx, cancel := context.WithCancel(context.Background())
```

### ❌ 错误做法
```go
// 错误：后台任务使用WithTimeout
ctx, cancel := context.WithTimeout(context.Background(), timeout)

// 错误：前台执行直接阻塞等待
output, err := cmd.CombinedOutput() // 会阻塞Agent
```

## 故障排除

- **后台任务输出为空**: 检查`TempFile`路径和goroutine写入逻辑
- **并发安全问题**: 验证锁的使用（读锁查询/写锁更新）
- **测试失败**: 清理临时文件（`Get-ChildItem -Path . -Recurse -Name "temp_*" -File | Remove-Item`）
- **PowerShell执行策略**: `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`
- **前台任务被阻塞**: 检查是否正确使用select等待结果或超时
- **后台任务意外终止**: 确认使用 `context.WithCancel` 而非 `WithTimeout`

## 重要设计决策

### 为什么前台超时要转后台？
1. **避免Agent阻塞**: Agent可以继续处理其他请求
2. **任务不会丢失**: 命令继续执行，用户可以通过bash_output查看
3. **用户体验更好**: 超时不等于失败，只是需要更长时间
4. **符合实际场景**: 编译、部署等任务可能需要不确定的时间

### 为什么后台任务不设超时？
1. **语义正确**: "后台执行"意味着"fire and forget"
2. **灵活性**: 用户可以运行任意长时间的任务
3. **主动控制**: 用户通过kill_shell主动终止，而非被动超时
4. **避免意外**: 长时间任务（如夜间构建）不会被意外终止
